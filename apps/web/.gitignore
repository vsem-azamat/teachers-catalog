node_modules/
dist/
dist-ssr/
*.local

# Certificates minted by vite-plugin-mkcert
.vite/
.mkcert/

# Types generated from the OpenAPI document (`pnpm api:generate`)
src/lib/generated/

# Catalogs compiled by `pnpm i18n:compile`. The build does not need them —
# @lingui/vite-plugin compiles the .po files on import — so they are a local
# convenience only. The .po files are the source of truth and are committed.
src/locales/*/messages.ts

.env
.env.*
!.env.example

*.log
.DS_Store
